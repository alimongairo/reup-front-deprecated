import cx from './index.module.scss';

const OrderPageHeader = () => {
  return (
    <div className={cx.wrapper}>
      <div className={cx.firstRow}>REUP</div>
    </div>
  );
};

export default OrderPageHeader;
