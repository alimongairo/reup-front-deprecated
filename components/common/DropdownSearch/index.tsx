import classNames from 'classnames';

import cx from './index.module.scss';

interface IProps {}

const DropdownSearch = ({}: IProps) => {
  return <div className={cx.wrapper}>DropdownSearch</div>;
};

export default DropdownSearch;
