import cx from './index.module.scss';

const BasketPageHeader = () => {
  return (
    <div className={cx.wrapper}>
      <div className={cx.firstRow}>Basket</div>
    </div>
  );
};

export default BasketPageHeader;
